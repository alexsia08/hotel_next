import styled from "@emotion/styled";

export const CSSFoterLine = styled.div(function () {
  return {
    // border: "2px solid red",
    width: "auto",
    height: "200px",
    // backgroundColor: "#f5f5dc",
  };
});
